---
# aixldap module data
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:
  - name: "common"
    path: "common.yaml"
lookup_options:
  aixldap::ldap_cfg_options:
    merge:
      strategy: deep
